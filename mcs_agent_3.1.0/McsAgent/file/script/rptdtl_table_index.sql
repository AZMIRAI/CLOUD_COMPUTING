# 월별 report_detail 테이블 생성 시 실행할 sql script를 입력. (index 등)
# 실행 시 '{RPTDTL_TABLE}' 문자열은 실제 report_detail 테이블명으로 치환됨.
# 각 스크립트는 ';' 로 종료되어야 함.
# [ex] create index ix_{RPTDTL_TABLE}_idx1 on {RPTDTL_TABLE} (field1) ;

